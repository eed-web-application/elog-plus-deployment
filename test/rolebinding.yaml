apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-reader-binding
  namespace: elog-plus
subjects:
- kind: ServiceAccount
  name: default
  namespace: elog-plus
roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io