apiVersion: v1
kind: ConfigMap
metadata:
  name: env-config-map
data:
  # property-like keys; each key maps to a simple value
  ELOG_PLUS_MAX_POST_SIZE: 100MB
  ELOG_PLUS_CONSUMER_GROUP_ID: elog-plus-group
  ELOG_PLUS_KAFKA_BOOTSTRAP: kafka:9092
  ELOG_PLUS_IMAGE_PREVIEW_TOPIC: elog-plus-preview-creation
  ELOG_PLUS_AUTHENTICATION_HEADER: X-Vouch-User
