apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: application-secrets
spec:
  path: secret/ad/accel-webapp-dev/elog/application-secrets
  type: Opaque
  keys:
    - ELOG_PLUS_APP_TOKEN_JWT_KEY
    - ELOG_PLUS_KAFKA_SASL_JAAS_CONFIG